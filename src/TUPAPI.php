<?php

/**
 * ----------------------------------------------------------------------------------
 * Tup API
 * ----------------------------------------------------------------------------------
 *
 * @author lanlin
 * @change 2021/06/01
 */
class TUPAPI
{
    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putBool(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return bool
     */
    public static function getBool(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): bool
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putChar(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return string
     */
    public static function getChar(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putUInt8(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return int
     */
    public static function getUInt8(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): int
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putShort(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return int
     */
    public static function getShort(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): int
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putUInt16(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return int
     */
    public static function getUInt16(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): int
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putInt32(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return int
     */
    public static function getInt32(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): int
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putUInt32(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return int
     */
    public static function getUInt32(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): int
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putInt64(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return int
     */
    public static function getInt64(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): int
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putDouble(string $name, mixed $value, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return float
     */
    public static function getDouble(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): float
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putFloat(string $name, mixed $value, int $iVersion = 3): string
    {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return float
     */
    public static function getFloat(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): float
    {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param mixed  $value
     * @param int    $iVersion
     *
     * @return string
     */
    public static function putString(string $name, mixed $value, int $iVersion = 3): string
    {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $name
     * @param string $buf
     * @param bool   $isRequire
     * @param int    $iVersion
     *
     * @return string
     */
    public static function getString(string $name, string $buf, bool $isRequire = false, int $iVersion = 3): string
    {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string       $name
     * @param \TARS_Vector $vector
     * @param int          $iVersion
     *
     * @return string
     */
    public static function putVector(string $name, TARS_Vector $vector, int $iVersion = 3): string
    {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string       $name
     * @param \TARS_Vector $vector
     * @param string       $buf
     * @param bool         $isRequire
     * @param int          $iVersion
     *
     * @return mixed
     */
    public static function getVector(
        string $name,
        TARS_Vector $vector,
        string $buf,
        bool $isRequire = false,
        int $iVersion = 3
    ): mixed {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string    $name
     * @param \TARS_Map $map
     * @param int       $iVersion
     *
     * @return string
     */
    public static function putMap(string $name, TARS_Map $map, int $iVersion = 3): string
    {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string    $name
     * @param \TARS_Map $map
     * @param string    $buf
     * @param bool      $isRequire
     * @param int       $iVersion
     *
     * @return array
     */
    public static function getMap(
        string $name,
        TARS_Map $map,
        string $buf,
        bool $isRequire = false,
        int $iVersion = 3,
    ): array {
        // ..
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string       $name
     * @param \TARS_Struct $struct
     * @param int          $iVersion
     *
     * @return string
     */
    public static function putStruct(string $name, TARS_Struct $struct, int $iVersion = 3): string
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string       $name
     * @param \TARS_Struct $struct
     * @param string       $buf
     * @param bool         $isRequire
     * @param int          $iVersion
     *
     * @return array
     */
    public static function getStruct(
        string $name,
        TARS_Struct $struct,
        string $buf,
        bool $isRequire = false,
        int $iVersion = 3
    ): array {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $respBuffer
     * @param int    $iVersion
     *
     * @return array
     */
    public static function decode(string $respBuffer, int $iVersion = 3): array
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param int    $iVersion
     * @param int    $iRequestId
     * @param string $servantName
     * @param string $funcName
     * @param int    $cPacketType
     * @param int    $iMessageType
     * @param int    $iTimeout
     * @param array  $contexts
     * @param array  $statuses
     * @param array  $inbufArr
     *
     * @return string
     */
    public static function encode(
        int $iVersion,
        int $iRequestId,
        string $servantName,
        string $funcName,
        int $cPacketType,
        int $iMessageType,
        int $iTimeout,
        array $contexts,
        array $statuses,
        array $inbufArr
    ): string {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param string $respBuffer
     *
     * @return array
     */
    public static function decodeReqPacket(string $respBuffer): array
    {
        // ...
    }

    // ------------------------------------------------------------------------------

    /**
     * @param int    $iVersion
     * @param int    $cPacketType
     * @param int    $iMessageType
     * @param int    $iRequestId
     * @param int    $iRet
     * @param string $sResultDesc
     * @param array  $inbufArr
     * @param array  $statuses
     *
     * @return string
     */
    public static function encodeRspPacket(
        int $iVersion,
        int $cPacketType,
        int $iMessageType,
        int $iRequestId,
        int $iRet,
        string $sResultDesc,
        array $inbufArr,
        array $statuses
    ): string {
        // ...
    }

    // ------------------------------------------------------------------------------
}
